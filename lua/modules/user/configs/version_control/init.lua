-- User plugin-config overrides.
-- Define functions on the returned `config` table to add or override setup for
-- the corresponding base plugins. Empty by default.

local config = {}

return config
